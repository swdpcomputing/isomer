#pragma once
#include "IsomerObject.h"

class NodeData
{
    public:
        NodeData() {};
        ~NodeData() {};
        
        
        
    private:
        JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR (NodeData);
};