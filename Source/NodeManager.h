#pragma once
#include "IsomerObject.h"

class NodeManager
{
    public:
        NodeManager() {};
        ~NodeManager() {};       
        
    private:
        JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR (NodeManager);
};